// Supabase client type is intentionally simple to keep service layers easy to read.
// The project still has strict row types in src/lib/types/db.ts.
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type SupabaseDBClient = any;
